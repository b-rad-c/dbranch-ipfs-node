FROM golang
WORKDIR /dbranch 

RUN git clone https://github.com/b-rad-c/dbranch-backend.git
WORKDIR dbranch-backend
RUN go build main.go
RUN mv main /usr/bin/dbranch-backend

WORKDIR /dbranch 
COPY curator.json .
CMD dbranch-backend daemon
